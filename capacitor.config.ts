import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.characteraicompanion',
  appName: 'Character AI Companion',
  webDir: 'dist/character-ai-companion'
};

export default config;
